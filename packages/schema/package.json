{
  "name": "@lrd/schema",
  "version": "0.1.0",
  "private": true,
  "description": "Zod schemas and types for the Lead Response Engine demo: RunFile, LeadPayload, NodeEvent, AttestFile, plus the label composer every surface renders from.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.4.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
